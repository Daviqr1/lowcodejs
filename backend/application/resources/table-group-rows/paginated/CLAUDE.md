# Paginated Group Rows

Lista paginada dos itens (subdocumentos) de um campo FIELD_GROUP.

## Endpoint
`GET /:slug/rows/:rowId/groups/:groupSlug/paginated` | Auth/Permission: VIEW_ROW

## Middlewares
`AuthenticationMiddleware` + a guarda de permissao indicada acima.
