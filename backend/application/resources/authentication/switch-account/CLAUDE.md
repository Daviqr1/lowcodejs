# Switch Account

Troca a conta ativa da sessao, reemitindo os cookies para o usuario-alvo vinculado.

## Endpoint
`POST /authentication/switch-account` | Auth/Permission: Auth

## Middlewares
`AuthenticationMiddleware` + a guarda de permissao indicada acima.
