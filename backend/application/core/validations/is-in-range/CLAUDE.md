# is-in-range — Regra de validação de campo

`export default new IsInRangeRule()` (`FieldValidationRule`, `requiresConfig`).
Valida faixa numérica via config `{ min?, max? }`. Regra pura/síncrona; ignora
valor vazio.

Contrato, registry e visão geral das 15 regras em [../CLAUDE.md](../CLAUDE.md).
