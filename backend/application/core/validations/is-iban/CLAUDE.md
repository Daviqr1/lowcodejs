# is-iban — Regra de validação de campo

`export default new IsIbanRule()` (`FieldValidationRule`). Valida formato IBAN.
Regra pura/síncrona; ignora valor vazio.

Contrato, registry e visão geral das 15 regras em [../CLAUDE.md](../CLAUDE.md).
