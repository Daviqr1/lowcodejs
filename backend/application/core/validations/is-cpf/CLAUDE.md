# is-cpf — Regra de validação de campo

`export default new IsCpfRule()` (`FieldValidationRule`). Valida CPF (regex de
`core/field-rules.core.ts`; migrada do `format` legado). Regra pura/síncrona;
ignora valor vazio.

Contrato, registry e visão geral das 15 regras em [../CLAUDE.md](../CLAUDE.md).
