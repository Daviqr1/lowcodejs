# is-email — Regra de validação de campo

`export default new IsEmailRule()` (`FieldValidationRule`). Valida formato de
e-mail (regex de `core/field-rules.core.ts`, fonte única). Regra pura/síncrona;
ignora valor vazio.

Contrato, registry e visão geral das 15 regras em [../CLAUDE.md](../CLAUDE.md).
