# is-numeric — Regra de validação de campo

`export default new IsNumericRule()` (`FieldValidationRule`). Valida valor
numérico (regex de `core/field-rules.core.ts`). Regra pura/síncrona; ignora
valor vazio.

Contrato, registry e visão geral das 15 regras em [../CLAUDE.md](../CLAUDE.md).
