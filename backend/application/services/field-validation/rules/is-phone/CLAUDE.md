# is-phone — Regra de validação de campo

`export default new IsPhoneRule()` (`FieldValidationRule`). Valida formato de
telefone (regex de `core/field-rules.core.ts`; migrada do `format` legado).
Regra pura/síncrona; ignora valor vazio.

Contrato, registry e visão geral das 15 regras em [../CLAUDE.md](../CLAUDE.md).
