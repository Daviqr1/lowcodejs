{
  "id": "date-window-guard",
  "type": "PLUGIN",
  "name": "Janela Temporal",
  "description": "Restringe visibilidade de rows por janela temporal — modo sliding (últimos N dias), fixed (intervalo de datas) ou field-range (vigência via campos da row). Admin sempre vê tudo.",
  "version": "1.0.0",
  "author": "Time Core",
  "icon": "CalendarClock",
  "placement": { "kind": "row-access-guard" },
  "requires": { "lowcodejs": ">=1.0.0" }
}
