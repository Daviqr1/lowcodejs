# print-table (plugin `core`, frontend)

Entry UI do plugin de referência: botão `window.print()` na toolbar da tabela.
Slot `table.actions`. `index.tsx` com `export default`.

Pacote `core` é auto-ativado no boot. Ver [../../CLAUDE.md](../../CLAUDE.md)
(`frontend/extensions/core/`) e `backend/extensions/core/plugins/`.
