# clone-table (tool `core`, frontend)

Entry UI da ferramenta de clonagem de tabelas (submenu Ferramentas,
`/tools/core/clone-table`). `index.tsx` com `export default` — formulário que
chama `POST /tools/clone-table`.

Pacote `core` é auto-ativado no boot. Ver [../../CLAUDE.md](../../CLAUDE.md)
(`frontend/extensions/core/`) e `backend/extensions/core/tools/clone-table/`.
