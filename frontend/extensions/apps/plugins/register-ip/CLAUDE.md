# register-ip (plugin `apps`, frontend)

Entry UI do plugin register-ip (pacote opcional `apps`). `index.tsx` com
`export default` — componente React montado no slot declarado pelo manifest.

Declaração canônica (manifest + slot) no backend. Ver a estrutura de pacotes em
[../../CLAUDE.md](../../CLAUDE.md) (`frontend/extensions/`).
