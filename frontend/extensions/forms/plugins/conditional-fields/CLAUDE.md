# conditional-fields (plugin `forms`, frontend)

Entry UI de configuração das regras de mostrar/ocultar campos e grupos no
formulário. Slot `table.fields.manage`. `index.tsx` com `export default`,
consumindo `/tables/:slug/config` e `.../runtime` do backend.

Ver [../../CLAUDE.md](../../CLAUDE.md) (`frontend/extensions/forms/`) e a
contraparte em `backend/extensions/forms/plugins/conditional-fields/`.
